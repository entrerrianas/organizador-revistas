#
# Defines navigation back to results for various types of "find" operations
#
ca_objects = {
	search => {
		module_path = ,
		controller = Search,
		action = getReturnToResultsUrl
	},
	browse => {
		module_path = ,
		controller = Browse,
		action = getReturnToResultsUrl
	},
	multisearch => {
		module_path = ,
		controller = MultiSearch,
		action = getReturnToResultsUrl
	},
	sets => {
		module_path = ,
		controller = Sets,
		action = Index,
		params = [set_id]
	}
}
ca_entities = {
	basic_search => {
		module_path = ,
		controller = Search,
		action = Index,
		params = [search]
	},
	advanced_search => {
		module_path = ,
		controller = AdvancedSearch,
		action = Index,
		params = []
	},
	basic_browse => {
		module_path = ,
		controller = Browse,
		action = Index,
		params = []
	}
}

ca_places = {
	basic_search => {
		module_path = ,
		controller = Search,
		action = Index,
		params = [search]
	},
	advanced_search => {
		module_path = ,
		controller = AdvancedSearch,
		action = Index,
		params = []
	},
	basic_browse => {
		module_path = ,
		controller = Browse,
		action = Index,
		params = []
	}
}

ca_occurrences = {
	basic_search => {
		module_path = ,
		controller = Search,
		action = Index,
		params = [search]
	},
	advanced_search => {
		module_path = ,
		controller = AdvancedSearch,
		action = Index,
		params = []
	},
	basic_browse => {
		module_path = ,
		controller = Browse,
		action = Index,
		params = []
	}
}

